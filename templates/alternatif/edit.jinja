<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>edit Alternatif</title>
  </head>
  <body>
    <p>edit alternatif</p>
    <form method="POST">
      {% for col in headers %}
        {% if col != 'kode_alternatif' %}
          {% if col == 'alternatif' %}
            <label for="{{ col }}">{{ col }}</label>
            <input type="text" name="{{ col }}" value="{{ data.get(col) }}" />
          {% else %}
            <label for="{{ col }}">{{ col }}</label>
            <input type="number" step="any" name="{{ col }}" value="{{ data.get(col) }}" />
          {% endif %}
        {% endif %}
      {% endfor %}
      <button type="submit">update</button>
    </form>
  </body>
</html>
